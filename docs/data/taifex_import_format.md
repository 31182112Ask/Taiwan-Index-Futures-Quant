# TAIFEX Import Format

The importer will support local CSV files and ZIP files containing CSV files under `data/raw/taifex`.

Column names may vary across TAIFEX exports, so later implementation should use a column mapping layer instead of a single hard-coded schema.

