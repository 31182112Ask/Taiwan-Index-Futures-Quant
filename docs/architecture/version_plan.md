# Version Plan

The project is developed in staged versions. The active version is V1 Backtest Lab.

V1 must remain a local historical research system. Broker APIs, live feeds, simulated broker order placement, and real-money order placement are out of scope.

